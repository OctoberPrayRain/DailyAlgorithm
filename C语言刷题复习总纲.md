# C 语言刷题复习总纲（可持续补充版）

> 使用方式：先按章节过一遍知识点，再结合题目在每章的「我的总结」里补充你的心得、易错点和模板。

---

## 0. 学习路线建议（刷题导向）

1. 语法与输入输出（1 天）
2. 数组 / 字符串 / 指针（3~5 天）
3. 函数 / 递归 / 结构体（2~3 天）
4. 排序 / 查找 / 常见题型套路（3~5 天）
5. 综合训练 + 复盘（长期）

## 0.1 本文档使用标准与风格基线

- 语言标准：默认按 `C11/C17` 兼容写法（至少 `C99`）。
- 风格基线：采用 **Linux kernel coding style** 思路，并做刷题场景简化。
- 编译建议：`gcc -std=c11 -Wall -Wextra -O2`

### Linux kernel 风格（刷题简化版）
- 缩进使用 **tab**，tab 宽度按 8 列理解。
- 单行长度尽量控制在 80 列左右（超出时合理换行）。
- 大括号风格使用 K&R：函数左括号另起行，控制语句左括号同行。
- 命名偏短且语义明确：`cnt`、`idx`、`left/right`，避免无意义单字母（循环变量 `i/j/k` 例外）。
- 指针星号靠变量名：`char *s`、`int *arr`。
- `if/for/while` 必须带大括号（刷题中可减少后续改动风险）。
- 优先早返回（early return），减少多层嵌套。
- 注释写“为什么”，不写“代码正在做什么”。
- 禁止魔法数字，常量用宏或 `const` 提取。

### 你可直接执行的自检清单
- [ ] 缩进是否统一（tab，不混空格缩进）
- [ ] 行宽是否明显过长（优先换行）
- [ ] 控制流是否都带 `{}` 
- [ ] 命名是否可读、可搜索
- [ ] 是否存在未使用变量/函数告警

---

## 1. C 程序基础结构

### 1.1 程序骨架
- `#include` 头文件
- `int main(void)` 或 `int main()`
- 局部变量、语句块、返回值 `return 0;`

### 1.2 编译与运行
- 常见命令：`gcc main.c -o main`、`./main`
- 常用选项：`-Wall -Wextra -O2 -g`

### 1.3 注释与代码风格
- 单行注释 `//`
- 多行注释 `/* ... */`
- 变量命名清晰、缩进统一、函数职责单一（详见 `0.1` 风格约定）

### 我的总结
- 

---

## 2. 数据类型、变量、常量

### 2.1 基本数据类型
- 整型：`short`、`int`、`long`、`long long`
- 字符：`char`
- 浮点：`float`、`double`
- 无符号：`unsigned int`、`unsigned long long`

### 2.2 修饰符与限定符
- `signed` / `unsigned`
- `const`（只读）
- `volatile`（了解）
- `static`（变量/函数作用域与生命周期）

### 2.3 变量作用域与生命周期
- 局部变量（栈）
- 全局变量（静态存储区）
- 静态局部变量（生命周期贯穿整个程序）

### 我的总结
- 

---

## 3. 输入输出（I/O）

### 3.1 标准输出 `printf`
- 格式控制：`%d`、`%lld`、`%u`、`%c`、`%s`、`%f`
- 转义字符：`\n`、`\t`

### 3.2 标准输入 `scanf`
- 基本读入：`scanf("%d", &x);`
- 多个变量：`scanf("%d%d", &a, &b);`
- 读字符时注意前置空白：`scanf(" %c", &ch);`

### 3.3 行输入
- `fgets(buf, sizeof(buf), stdin)`（推荐）
- `sscanf` 做二次解析

### 3.4 常见坑
- 忘记取地址符 `&`
- `%d` 和 `%lld` 混用
- 缓冲区残留换行导致字符读入异常

### 我的总结
- 

---

## 4. 运算符与表达式

### 4.1 算术/关系/逻辑运算
- `+ - * / %`
- `> < >= <= == !=`
- `&& || !`

### 4.2 赋值与自增
- `=`、`+=`、`-=`
- `i++` 与 `++i`

### 4.3 位运算（刷题高频）
- `& | ^ ~ << >>`
- 常见用途：判奇偶、取第 k 位、状态压缩入门

### 4.4 运算符优先级
- 复杂表达式强制加括号，降低出错概率

### 我的总结
- 

---

## 5. 流程控制

### 5.1 条件语句
- `if / else if / else`
- `switch / case / default`

### 5.2 循环语句
- `for`（计数型）
- `while`（条件型）
- `do...while`（至少执行一次）

### 5.3 循环控制
- `break`、`continue`

### 5.4 常见坑
- 死循环
- 边界写错：`i < n` vs `i <= n`

### 我的总结
- 

---

## 6. 函数

### 6.1 函数定义与声明
- 函数原型（声明）与实现分离
- 返回值类型与参数类型匹配

### 6.2 参数传递
- C 语言默认值传递
- 需要修改实参时传地址（指针）

### 6.3 递归
- 终止条件
- 递推关系
- 避免重复计算和栈过深

### 6.4 常见库函数
- `math.h`：`abs`、`sqrt`、`pow`
- `string.h`：`strlen`、`strcmp` 等

### 我的总结
- 

---

## 7. 数组（重点）

### 7.1 一维数组
- 定义、初始化、遍历
- 下标从 `0` 开始
- 连续内存结构

### 7.2 二维数组
- 定义：`int a[n][m]`
- 行优先存储（按行连续）

### 7.3 典型题型
- 遍历统计、最大最小值
- 前缀和
- 双指针
- 滑动窗口

### 7.4 高频坑点
- 越界访问
- 忘记初始化（如计数数组）
- 数据范围导致 `int` 溢出

### 我的总结
- 

---

## 8. 字符串（重点）

### 8.1 字符串本质
- `char` 数组 + `'\0'` 结尾

### 8.2 常见操作
- 长度：`strlen`
- 比较：`strcmp`
- 复制：`strcpy` / `strncpy`
- 拼接：`strcat` / `strncat`
- 查找：`strchr`、`strstr`

### 8.3 输入处理
- `scanf("%s", s)` 不读空格
- `fgets` 可读空格但可能包含换行

### 8.4 高频坑点
- 空间不足（忘留 `'\0'`）
- 字符串与字符比较混淆（`"a"` vs `'a'`）
- 修改字符串字面量导致未定义行为

### 我的总结
- 

---

## 9. 指针（重点）

### 9.1 基本概念
- 地址、解引用：`&` 与 `*`
- 指针类型决定步长和解释方式

### 9.2 指针与数组
- 数组名常退化为首元素地址
- `a[i] == *(a + i)`

### 9.3 指针与函数
- 通过指针修改外部变量
- 传数组本质上是传首地址

### 9.4 多级指针
- `int **pp` 基本理解与常见传参场景

### 9.5 高频坑点
- 野指针、空指针解引用
- 返回局部变量地址
- `*p++` 与 `(*p)++` 混淆

### 我的总结
- 

---

## 10. 结构体、枚举、共用体

### 10.1 结构体 `struct`
- 定义与初始化
- 访问：`.` 与 `->`
- 用于封装记录类数据（如区间、节点、学生信息）

### 10.2 枚举 `enum`
- 定义离散状态，提高可读性

### 10.3 共用体 `union`（了解）
- 多成员共享同一段内存

### 我的总结
- 

---

## 11. 动态内存管理

### 11.1 常用函数（`stdlib.h`）
- `malloc`、`calloc`、`realloc`、`free`

### 11.2 使用规范
- 申请后判空
- 使用完及时 `free`
- 释放后置 `NULL`（好习惯）

### 11.3 高频坑点
- 内存泄漏
- 重复释放
- 越界写导致崩溃

### 我的总结
- 

---

## 12. 预处理与宏

### 12.1 宏定义
- `#define PI 3.1415926`
- 带参宏注意加括号：`#define MAX(a,b) ((a)>(b)?(a):(b))`

### 12.2 条件编译
- `#ifdef #ifndef #endif`

### 12.3 头文件保护
- `#ifndef XXX_H ... #define XXX_H ... #endif`

### 我的总结
- 

---

## 13. 文件操作（按需）

### 13.1 基本流程
- `fopen` -> `fgets/fscanf/fprintf` -> `fclose`

### 13.2 常见模式
- 文本读写
- 结果落盘日志

### 13.3 高频坑点
- 文件打开失败未判断
- 模式不匹配（`r/w/a`）

### 我的总结
- 

---

## 14. 调试与错误排查

### 14.1 编译期
- 打开警告：`-Wall -Wextra`
- 优先消除 warning

### 14.2 运行期
- 打印中间变量定位逻辑错误
- 边界输入自测：空、极值、重复、逆序

### 14.3 常见错误类型
- 段错误（越界/非法指针）
- 无限循环
- 精度或溢出问题

### 我的总结
- 

---

## 15. 刷题常见算法专题（C 实现视角）

### 15.1 基础专题
- 模拟
- 枚举
- 前缀和 / 差分
- 双指针
- 滑动窗口
- 哈希计数（数组映射）

### 15.2 进阶专题
- 排序（快速/归并/堆）
- 二分答案
- 贪心
- 栈与队列
- 链表
- 二叉树基础遍历
- 回溯
- 动态规划入门

### 15.3 C 语言实现注意点
- 手写数据结构时的边界与初始化
- 函数拆分，减少主流程复杂度
- 优先写可验证的朴素版本，再优化

### 我的总结
- 

---

## 16. 刷题模板区（可自行扩展）

### 16.1 通用输入模板
```c
#include <stdio.h>

int main(void) {
    int n;
    if (scanf("%d", &n) != 1) return 0;
    for (int i = 0; i < n; i++) {
        // ...
    }
    return 0;
}
```

### 16.2 多组数据模板
```c
while (scanf("%d", &n) == 1) {
    // solve
}
```

### 16.3 数组初始化模板
```c
int cnt[1005] = {0};
```

### 16.4 字符串读取模板
```c
char s[1005];
if (fgets(s, sizeof(s), stdin)) {
    // ...
}
```

### 我的总结
- 

---

## 17. 错题复盘模板（每题一条）

- 题目：
- 标签：
- 我的思路：
- 正确做法：
- 我错在哪：
- 关键边界：
- 可复用代码片段：
- 复盘日期：

---

## 18. 周复习模板

- 本周完成题数：
- 主要题型：
- 新掌握的技巧：
- 重复出错点：
- 下周修正计划：

---

## 19. 阶段检查清单（打勾）

- [ ] 我能熟练处理数组边界
- [ ] 我能写出安全字符串处理代码
- [ ] 我能正确使用指针传参
- [ ] 我能独立写出常见排序/二分
- [ ] 我能在 30 分钟内完成一道中等模拟题
- [ ] 我有自己的错题模板与复盘节奏

---

## 20. 你的个性化补充区

- 常用模板 1：
- 常用模板 2：
- 个人高频坑：
- 面试常问点：
- 最后总复习日期：
