// 输入 n 和长度为 n 的整数数组，输出 max-min

#include <stdio.h>
#include <stdlib.h>

int main() {
        int n;
        scanf("%d", &n);

        int a[n];

        for (int i = 0; i < n; i ++) {
                (*a) += i;
        }
}

